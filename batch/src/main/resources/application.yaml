server:
  port: 8003
  servlet:
    context-path: /batch

spring:
  application:
    name: batch
  datasource:
    url: jdbc:mysql://43.138.26.5:3306/train_batch?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&autoReconnect=true
    username: train_batch
    password: Sss030323
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/**/*Mapper.xml

logging:
  level:
    com.sss.train.member.mapper: trace
